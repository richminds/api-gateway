# ── HTTP service ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ── Upstream calls (auth-service client + the reverse proxy itself) ──────────
# httpx is load-bearing here in a way it isn't in the sibling services: every
# proxied request is an httpx stream, so the connection-pool settings in
# features/proxy.py are a capacity knob, not a detail.
httpx>=0.27

# ── Persistence (usage counters) ─────────────────────────────────────────────
# Optional at runtime: with no GATEWAY_MONGO_URI the counters stay in-process
# (see features/usage.py). Tokens are validated by auth-service, so the gateway
# needs no JWT library and no signing key of its own.
motor>=3.3
pymongo>=4.6
